<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>

    <declare-styleable name="TagsFlowView">
        <attr name="tfv_style" format="enum">
            <enum name="normal" value="0"/>
            <enum name="small" value="1"/>
        </attr>
        <attr name="tfv_cell_clickable" format="boolean"/>
        <attr name="tfv_max_lines" format="integer"/>
    </declare-styleable>

    <declare-styleable name="GradientTextView">
        <attr name="gtv_startColor" format="color" />
        <attr name="gtv_endColor" format="color" />
        <attr name="gtv_angle" format="float" />
    </declare-styleable>

    <declare-styleable name="SpoilerParticleView">
        <attr name="spv_cornerRadius" format="dimension" />
    </declare-styleable>

    <declare-styleable name="SegmentedToggleLayout">
        <attr name="stl_textSize" format="dimension" />
    </declare-styleable>

    <!-- Chat subsystem (borrowed from Peanut; see ceui.pixiv.chat.base.widget.EmptyStateView) -->
    <declare-styleable name="EmptyStateView">
        <attr name="es_icon" format="reference" />
        <attr name="es_title" format="string" />
        <attr name="es_subtitle" format="string" />
        <attr name="es_actionText" format="string" />
    </declare-styleable>

</resources>
