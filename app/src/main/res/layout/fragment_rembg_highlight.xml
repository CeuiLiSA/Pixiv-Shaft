<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <!-- Subject highlight view -->
    <ceui.pixiv.ui.upscale.SubjectHighlightView
        android:id="@+id/subject_highlight"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Top-left label badge -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|start"
        android:layout_margin="16dp"
        android:background="@drawable/bg_rate_button_primary"
        android:paddingHorizontal="10dp"
        android:paddingVertical="4dp"
        android:text="@string/string_ai_rembg"
        android:textColor="@color/always_white"
        android:textSize="11sp" />

    <!-- Status hint -->
    <TextView
        android:id="@+id/hint_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|center_horizontal"
        android:layout_marginTop="20dp"
        android:alpha="0"
        android:text="@string/string_rembg_subject_detected"
        android:textColor="#B0FFFFFF"
        android:textSize="13sp" />

    <!-- Bottom action bar -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="40dp"
        android:gravity="center"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/btn_preview"
            android:layout_width="wrap_content"
            android:layout_height="32dp"
            android:layout_marginEnd="12dp"
            android:background="@drawable/refresh_button_background"
            android:gravity="center"
            android:paddingHorizontal="16dp"
            android:stateListAnimator="@animator/button_press_alpha"
            android:text="@string/string_rembg_preview"
            android:textAllCaps="false"
            android:textColor="@android:color/white"
            android:textSize="13sp" />

        <TextView
            android:id="@+id/btn_save"
            style="@style/GreenShiningButton"
            android:layout_width="wrap_content"
            android:layout_height="32dp"
            android:paddingHorizontal="16dp"
            android:text="@string/string_rembg_save" />

    </LinearLayout>

</FrameLayout>
