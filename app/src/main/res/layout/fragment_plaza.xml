<?xml version="1.0" encoding="utf-8"?>
<!-- Toolbar 与设置页共用主题和 inset；Material3 仅应用于内容区。 -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/v3_bg"
    android:orientation="vertical">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fitsSystemWindows="true"
        android:background="?attr/colorPrimary"
        android:elevation="@dimen/six_dp"
        android:theme="@style/AppTheme.AppBarOverlay"
        app:popupTheme="@style/AppTheme.PopupOverlay"
        app:menu="@menu/plaza_toolbar_menu"
        app:navigationIcon="@drawable/ic_arrow_back_white_shadow"
        app:title=" "
        app:titleTextColor="@android:color/white">

        <TextView
            android:id="@+id/toolbar_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="@string/plaza_title"
            android:textColor="@color/white"
            android:textSize="18sp"
            tools:text="广场" />
    </androidx.appcompat.widget.Toolbar>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:theme="@style/Theme.Material3.DayNight.NoActionBar">

        <TextView
            android:id="@+id/feed_intro"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:paddingStart="20dp"
            android:paddingTop="8dp"
            android:paddingEnd="20dp"
            android:paddingBottom="12dp"
            android:text="@string/plaza_feed_description"
            android:textColor="@color/v3_text_2"
            android:textSize="14sp"
            android:fontFamily="@font/montserrat_regular"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <ProgressBar
            android:id="@+id/initial_loading"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:contentDescription="@string/plaza_loading"
            android:visibility="gone"
            app:layout_constraintTop_toTopOf="@id/refresh_layout"
            app:layout_constraintBottom_toBottomOf="@id/refresh_layout"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
            android:id="@+id/refresh_layout"
            android:layout_width="0dp"
            android:layout_height="0dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/feed_intro">

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/recycler_view"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:clipToPadding="false"
                android:paddingStart="16dp"
                android:paddingEnd="16dp"
                android:paddingTop="4dp"
                android:paddingBottom="100dp" />
        </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

        <!-- 空状态：广场里没帖子时显示，按 ViewModel 状态切显隐 -->
        <TextView
            android:id="@+id/empty_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/plaza_no_posts"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:textSize="14sp"
            android:visibility="gone"
            app:layout_constraintBottom_toBottomOf="@id/refresh_layout"
            app:layout_constraintEnd_toEndOf="@id/refresh_layout"
            app:layout_constraintStart_toStartOf="@id/refresh_layout"
            app:layout_constraintTop_toTopOf="@id/refresh_layout"
            tools:visibility="visible" />

        <!-- 错误状态：首次加载失败时显示。文案 + 重试按钮,跟下拉刷新双兜底。
             items 已有数据时不显示(改用 toast),只在白屏时占位。 -->
        <LinearLayout
            android:id="@+id/error_layout"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="vertical"
            android:paddingStart="32dp"
            android:paddingEnd="32dp"
            android:visibility="gone"
            app:layout_constraintBottom_toBottomOf="@id/refresh_layout"
            app:layout_constraintEnd_toEndOf="@id/refresh_layout"
            app:layout_constraintStart_toStartOf="@id/refresh_layout"
            app:layout_constraintTop_toTopOf="@id/refresh_layout"
            tools:visibility="visible">

            <TextView
                android:id="@+id/error_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:textColor="?attr/colorOnSurfaceVariant"
                android:textSize="14sp"
                tools:text="加载失败:网络不可用" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/error_retry"
                style="@style/Widget.Material3.Button.TonalButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:text="@string/retry" />

        </LinearLayout>

        <!-- FAB —— 跟顶部 + 双入口设计，移动端用户更习惯右下 FAB 发帖 -->
        <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton
            android:id="@+id/fab_compose"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="20dp"
            android:layout_marginBottom="24dp"
            android:contentDescription="@string/plaza_compose_title"
            android:text="@string/plaza_compose_title"
            android:fontFamily="@font/montserrat_semi_bold"
            app:icon="@drawable/ic_add_black_24dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:cornerRadius="28dp" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</LinearLayout>
