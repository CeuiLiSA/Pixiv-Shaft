<?xml version="1.0" encoding="utf-8"?>
<!-- 长按消息的操作菜单,坐在 WitBottomSheet 的主题表面上(拖拽条、安全区由它管)。
     结构:消息引用块 → 复制 / 回复 / 转发 连通分段行(外角 20、内角 5、行隙 2)→ 独立的删除行。
     所有底色与强调色由 MessageActionsSheet 按聊天主题色运行时铺,日夜跟随资源。 -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="16dp"
    android:paddingTop="4dp"
    android:paddingEnd="16dp"
    android:paddingBottom="16dp">

    <!-- 与输入栏引用条同一语汇:3dp 主题色强调条 + 主题浅底容器。 -->
    <LinearLayout
        android:id="@+id/preview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingStart="12dp"
        android:paddingTop="12dp"
        android:paddingEnd="16dp"
        android:paddingBottom="12dp">

        <View
            android:id="@+id/preview_accent"
            android:layout_width="3dp"
            android:layout_height="match_parent" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tv_sender"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="4dp"
                android:ellipsize="end"
                android:includeFontPadding="false"
                android:singleLine="true"
                android:textSize="13sp"
                android:visibility="gone"
                tools:text="匿名_95864319"
                tools:visibility="visible" />

            <TextView
                android:id="@+id/tv_preview"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:lineSpacingMultiplier="1.3"
                android:maxLines="3"
                android:textColor="@color/v3_text_1"
                android:textSize="15sp"
                tools:text="晚上睡不着来把战争雷霆越玩越精神" />

        </LinearLayout>

    </LinearLayout>

    <LinearLayout
        android:id="@+id/action_group"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:orientation="vertical">

        <include
            android:id="@+id/action_copy"
            layout="@layout/chat_item_message_action" />

        <include
            android:id="@+id/action_reply"
            layout="@layout/chat_item_message_action" />

        <include
            android:id="@+id/action_forward"
            layout="@layout/chat_item_message_action" />

    </LinearLayout>

    <!-- 破坏性操作单独成组,和常用操作隔开一段,避免顺手误点。 -->
    <include
        android:id="@+id/action_delete"
        layout="@layout/chat_item_message_action"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp" />

</LinearLayout>
