<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingHorizontal="20dp"
    android:paddingTop="10dp"
    android:paddingBottom="16dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:background="?attr/selectableItemBackground"
            android:letterSpacing="-0.03"
            android:lineSpacingMultiplier="1.2"
            android:textColor="@color/v3_text_1"
            android:textSize="23sp"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/bookmark"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="ある小説シリーズ" />

        <ceui.pixiv.widgets.ProgressImageButton
            android:id="@+id/bookmark"
            android:layout_width="@dimen/min_widget_size"
            android:layout_height="@dimen/min_widget_size"
            android:padding="8dp"
            android:src="@drawable/icon_not_liked"
            app:layout_constraintBottom_toBottomOf="@+id/title"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@+id/title"
            app:ptb_progress_stroke_width="3dp"
            app:ptb_progress_width="14dp" />
    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- Meta badge: 醒目一点的 V3 胶囊（type · 篇数 · 字数），底色/文字色跑
         V3Palette 主题色 tint，日夜自适应。文案由 ViewHolder 拼。 -->
    <TextView
        android:id="@+id/meta_badge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:letterSpacing="0.01"
        android:paddingHorizontal="12dp"
        android:paddingVertical="5dp"
        android:singleLine="true"
        android:textSize="13sp"
        android:textStyle="bold"
        tools:text="Novel · 19篇 · 136,969字" />

    <!-- "阅读最新一话(#N)" — 旧 FragmentNovelSeriesDetail 里很显眼的入口，
         迁到新页 hero 卡片底部。仅当系列有最新章节时（latestNovelChapterIndex
         非 null）由 ViewHolder 切换可见。 -->
    <TextView
        android:id="@+id/read_latest"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginTop="14dp"
        android:background="@drawable/v3_glass_surface"
        android:gravity="center"
        android:textColor="@color/v3_text_1"
        android:textSize="13sp"
        android:textStyle="bold"
        android:visibility="gone"
        tools:text="阅读最新一话(#12)"
        tools:visibility="visible" />
</LinearLayout>
